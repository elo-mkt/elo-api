{
  "query": "query TableOfBins {
  binTable {

    sizeRange {
      min
      max
    }

    bins {

      number

      funding

      isInternational

      isToken

      product {
        name
      }

      allowedCaptures {
        name
        code
      }

      usages {
        name
        code
      }

      services {
        name
      }

      issuer {
        id
        name
        legalName
      }
    }
  }
}",
  "variables": null
}
