{
	"query":"query OneBin {
      bin(number: \"$bin\") {

        issuer {
          name
        }

        product {
          name
        }

        allowedCaptures {
          name
          code
        }

        usages {
          name
          code
        }

        services {
          name
        }
  }
}",
	"variables":null
}