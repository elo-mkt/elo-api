{
	"query":"query OneBin {
      bin(number: \"$bin\") {

        issuer {
          name
        }

        product {
          id
          name
        }

        allowedCaptures {
          name
          code
        }

        usages {
          name
          code
        }

        services {
          name
        }
  }
}",
	"variables":null
}